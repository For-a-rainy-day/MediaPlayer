<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_navigation"
    app:startDestination="@id/fragmentMusic">

    <fragment
        android:id="@+id/fragmentMusic"
        android:name="com.example.mediplayer.FragmentMusic"
        android:label="fragment_music"
        tools:layout="@layout/fragment_music" >
        <action
            android:id="@+id/action_fragmentMusic_to_fragmentMenu"
            app:destination="@id/fragmentMenu" />
    </fragment>
    <fragment
        android:id="@+id/fragmentMenu"
        android:name="com.example.mediplayer.FragmentMenu"
        android:label="fragment_menu"
        tools:layout="@layout/fragment_menu" />
</navigation>